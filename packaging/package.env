# Safelibs port metadata for libyaml.
#
# The actual Debian package metadata for this port lives in safe/ and is
# consumed by scripts/build-debs.sh. The only template-required field is
# SAFELIBS_LIBRARY, which the validator hook and check-layout consume.

SAFELIBS_LIBRARY="libyaml"
